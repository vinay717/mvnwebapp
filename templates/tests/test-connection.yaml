apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mvnwebapp.fullname" . }}-test-connection"
  labels:
    {{- include "mvnwebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mvnwebapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
