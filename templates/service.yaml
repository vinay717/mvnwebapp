apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvnwebapp.fullname" . }}
  labels:
    {{- include "mvnwebapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvnwebapp.selectorLabels" . | nindent 4 }}
